import SocialSecurityElement from './SocialSecurityElement';

export const SocialSecurityElementConfig = {
    settings: {
        layout: {
            config: {}
        }
    },
    routes  : [
        {
            path     : '/socialsecurityelement',
            component: SocialSecurityElement
        }
    ]
};
